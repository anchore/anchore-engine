# source this file if you would like to run functional tests against a local running instance of engine
# that is not within the standard functional compose stack used in CI (make setup-and-test-functional)

# note: you may need to change these values to meet your setup

export ANCHORE_TEST_DOCKER_REGISTRY_USER=anchore
export ANCHORE_TEST_DOCKER_REGISTRY_PASS=foobar
export ANCHORE_TEST_DOCKER_REGISTRY_HOST=localhost:5000
export ANCHORE_BASE_URL=http://localhost:8228/v1
export ANCHORE_CLI_URL=http://localhost:8228/v1
export ANCHORE_CLI_USER=admin
export ANCHORE_CLI_PASS=foobar
export TOX_ENV=py36
